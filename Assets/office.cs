using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class office : MonoBehaviour
{
    public Vector3[] officelist;
    public bool day;
    // Start is called before the first frame update
    void Start()
    {
        day = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
